name: CICD

on:
  push:
    paths-ignore:
      - ".github/CODEOWNERS"
      - .gitignore
      - "**/*.md"

jobs:

  build:
    uses: Eqwerty/CICD-workflows/.github/workflows/build.yml@main
    with:
      build-artifacts: ${{ github.ref_name == 'master' }}
  
  deploy-development:
    if: ${{ github.ref_name == 'master' }}
    uses: Eqwerty/CICD-workflows/.github/workflows/deploy.yml@main
    needs: [ build ]
    secrets: inherit
    with:
      environmentName: Development
  
  deploy-staging:
    uses: Eqwerty/CICD-workflows/.github/workflows/deploy.yml@main
    needs: [ deploy-development ]
    secrets: inherit
    with:
      environmentName: Staging
  
  deploy-production:
    uses: Eqwerty/CICD-workflows/.github/workflows/deploy.yml@main
    needs: [ deploy-staging ]
    secrets: inherit
    with:
      environmentName: Production